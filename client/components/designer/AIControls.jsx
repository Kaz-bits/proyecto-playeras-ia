import React from 'react';

const AIControls = () => {
  return (
    <div className="ai-controls">
      <h3>Controles de IA</h3>
      {/* Aquí irán los controles para generar diseños con IA */}
    </div>
  );
};

export default AIControls;
